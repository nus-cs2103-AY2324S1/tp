package seedu.address.logic.parser;

import static org.junit.jupiter.api.Assertions.*;

// partition based on if the input has valid index and valid description.
// addtask with index, without index when no current lesson is shown , without index when index is invalid
class AddTaskCommandParserTest {

}