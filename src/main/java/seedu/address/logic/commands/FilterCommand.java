package seedu.address.logic.commands;
/**
 * Represents a command that filters the list of students or lessons.
 */
public abstract class FilterCommand extends Command {
    public static final String COMMAND_WORD = "filter";
}
